require 'rake-pipeline-web-filters'

output "static"

input "templates/clientside" do
  match "**/*.handlebars" do
    handlebars :key_name_proc => proc { |input| input.path.split(File.extname(input.path))[0] }
    concat "javascripts/templates.js"
  end
end
